Place protobuf contracts here (internal service-to-service). Keep them backward compatible.
Example structure:
  proto/auth/v1/auth.proto
  proto/catalog/v1/catalog.proto
Generate code into ./gen/... or per-service internal packages.
